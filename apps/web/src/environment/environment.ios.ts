import { AppEnvironmentConfig } from '@repo/core'

export const environment: AppEnvironmentConfig = {
    appName: 'MyApp',
    platform: 'ios',
    production: true,
    apiUrl: 'https://api.touhidrahman.me',
    authApiUrl: 'https://api.touhidrahman.me/v1',
}
