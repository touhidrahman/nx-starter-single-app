import { Component } from '@angular/core'

import { PrimeModules } from '@repo/prime-modules'

@Component({
    selector: 'app-page-organization-status-pending',
    imports: [PrimeModules],
    templateUrl: './page-organization-status-pending.html',
    styleUrl: './page-organization-status-pending.scss',
})
export class pageOrganizationStatusPendingComponent {
    // private router = inject(Router)
    // navigateToLogin() {
    //     this.router.navigate(['/login'])
    // }
}
